import 'package:flutter/material.dart';

class MatchesScreen extends StatelessWidget {
  const MatchesScreen({super.key});
  static String id = 'matches';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
