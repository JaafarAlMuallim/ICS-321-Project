import 'package:flutter/material.dart';

class TeamInfoScreen extends StatelessWidget {
  const TeamInfoScreen({super.key});
  static String id = 'teamInfo';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
